# Database configuration
db.host=localhost
db.port=3306
db.user=root
db.password=
db.database=xjournal

# Other configuration properties can go here, if needed
# e.g., connection timeout, pool size, etc.
db.connectTimeout=60000
db.useSSL=false